import { Box, Typography } from "@mui/material";
import React from "react";

const Register: React.FC = () => {
  return (
    <Box>
      <Typography>Register Works.</Typography>
    </Box>
  );
};

export default Register;
