import { Box, Typography } from "@mui/material";
import React from "react";
const Header: React.FC = () => {
  return (
    <Box>
      <Typography>Header Wroks</Typography>
    </Box>
  );
};

export default Header;
